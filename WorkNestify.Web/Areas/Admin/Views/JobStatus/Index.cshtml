@model IEnumerable<WorkNestify.DataAccess.Entities.Jobs.JobStatus>

@{
    ViewData["Title"] = "Job Statuses Management";
}

<partial name="_ManagementNavigationPartial"/>

<table id="jobStatusesTable" class="table table-striped table-hover nowrap w-100">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section Scripts
{
    @{ await Html.RenderPartialAsync("_DataTablesScriptsPartial"); }
    <script asp-append-version="true">
        $(document).ready(function () {
            $('#jobStatusesTable').DataTable({
                ajax: {
                    url: '/Admin/JobStatus/GetAll',
                    dataSrc: 'data'
                },
                columns: [
                    { data: 'name' },
                    {
                        data: 'id',
                        render: function (data) {
                            return `<div class="btn-group" role="group">
                                <a href="/Admin/JobStatus/Details/${data}" class="btn btn-info"> <i class="bi bi-info-circle me-2"></i>Details</a>
                            </div>`;
                        },
                        orderable: false,
                        searchable: false
                    }
                ],
                responsive: true,
                dom: 'Bfrtip',
                buttons: [
                    'copy', 'csv', 'excel', 'pdf', 'print'
                ],
                language: {
                    emptyTable: "No jobs found"
                }
            });
        });
    </script>
}